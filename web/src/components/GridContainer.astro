<style>
    .grid-container {
        width: 100%;
        display: flex;
        /* max of 333px per element */
        margin-inline: auto;
        justify-content: center;
        flex-wrap: wrap;
        grid-gap: 40px;
    }
    .grid-container > div {
        max-width: 500px;
    }

    /* if phone, 1 col
	@media (max-width: 600px) {
		.container {
			grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
		}
		.container > * {
			width: 80%;
			height: 80%;
		}
	} */
</style>

<div class="grid-container">
    <slot />
</div>
